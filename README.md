# 🥩 Классификатор свежести мяса

Приложение для определения свежести мяса с помощью нейронной сети на основе ResNet-18.

## 📋 Требования

- Python 3.8 или выше
- Модель `meat_classifier.pth` (должна быть в корневой директории проекта)

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Примечание:** PyTorch будет установлен с поддержкой CPU по умолчанию. Это идеально для инференса - модель работает быстро даже на CPU. Если у вас есть GPU и вы хотите использовать его для обучения (но не обязательно для инференса), установите версию PyTorch с поддержкой CUDA с [официального сайта](https://pytorch.org/).

### 2. (Опционально) Проверка окружения

Перед запуском можно проверить, что всё настроено правильно:

```bash
python check_setup.py
```

Скрипт проверит версию Python, наличие всех зависимостей и файла модели.

### 3. Запуск приложения

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## 📁 Структура проекта

```
neuro/
├── app.py                 # Основное приложение Streamlit
├── main.py                # Скрипт для обучения модели
├── meat_classifier.pth    # Обученная модель (необходима для работы)
├── requirements.txt       # Зависимости Python
├── dataset/               # Датасет для обучения (не требуется для инференса)
│   ├── train/
│   └── validation/
└── results/               # Директория для сохранения результатов (создается автоматически)
    └── predictions.json   # История предсказаний
```

## 💻 Использование

1. Запустите приложение командой `streamlit run app.py`
2. Загрузите изображение мяса (JPG, JPEG, PNG, BMP, WEBP)
3. Нажмите кнопку "Проверить свежесть мяса"
4. Получите результат: свежее мясо ✅ или несвежее ⚠️
5. Все результаты автоматически сохраняются в `results/predictions.json`

## 🔧 Особенности

- ✅ Работает на CPU (не требует GPU для инференса)
- ✅ Автоматическая проверка наличия обученной модели
- ✅ Сохранение всех результатов предсказаний
- ✅ История проверок с возможностью просмотра
- ✅ Поддержка множества форматов изображений

## 📦 Что нужно для работы на другом ПК

Минимальный набор файлов для развертывания:
- `app.py` - основное приложение
- `meat_classifier.pth` - обученная модель (обязательно!)
- `requirements.txt` - список зависимостей
- `check_setup.py` - скрипт проверки (опционально)
- `README.md` - документация (опционально)

### Быстрый старт на новом ПК:

1. Скопируйте файлы: `app.py`, `meat_classifier.pth`, `requirements.txt`
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите приложение: `streamlit run app.py`

**Важно:** Модель работает на CPU, поэтому не требуется GPU или CUDA драйверы. Приложение будет работать на любом ПК с Python 3.8+.

## ⚠️ Важно

- Модель работает на CPU, поэтому не требует GPU или CUDA
- Файл модели `meat_classifier.pth` должен находиться в той же директории, что и `app.py`
- Для обучения модели используйте `main.py` (требует GPU и датасет)

## 🐛 Решение проблем

**Ошибка "Модель не обучена":**
- Убедитесь, что файл `meat_classifier.pth` находится в корневой директории проекта
- Проверьте, что файл не поврежден

**Ошибка импорта модулей:**
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте версию Python: `python --version` (должна быть 3.8+)

