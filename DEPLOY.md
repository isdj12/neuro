# 🚀 Инструкция по развертыванию на другом ПК

## Минимальные файлы для работы

Скопируйте эти файлы на другой ПК:

```
✅ app.py                 - основное приложение
✅ meat_classifier.pth    - обученная модель (43 МБ)
✅ requirements.txt       - зависимости
```

## Шаги установки

### 1. Установите Python 3.8+ (если еще не установлен)

Проверьте версию:
```bash
python --version
# или
python3 --version
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

Это установит:
- Streamlit (веб-интерфейс)
- PyTorch (CPU версия, работает без GPU)
- Torchvision
- Pillow (обработка изображений)

**Время установки:** ~5-10 минут (зависит от скорости интернета)

### 3. Запустите приложение

```bash
streamlit run app.py
```

Приложение автоматически откроется в браузере на `http://localhost:8501`

## ✅ Проверка работоспособности

После установки можно запустить проверку:

```bash
python check_setup.py
```

Скрипт проверит:
- Версию Python
- Наличие всех библиотек
- Наличие файла модели

## 💡 Важные моменты

1. **GPU не требуется** - модель работает на CPU
2. **Файл модели обязателен** - без `meat_classifier.pth` приложение не запустится
3. **Порты** - убедитесь, что порт 8501 свободен (или измените в Streamlit)
4. **Интернет** - нужен только для установки зависимостей, не для работы

## 🔧 Решение проблем

**Ошибка "Модуль не найден":**
```bash
pip install --upgrade -r requirements.txt
```

**Ошибка "Модель не найдена":**
- Убедитесь, что `meat_classifier.pth` в той же папке, что и `app.py`
- Проверьте права доступа к файлу

**Приложение не открывается в браузере:**
- Откройте вручную: `http://localhost:8501`
- Или используйте: `http://127.0.0.1:8501`

## 📊 Системные требования

- **ОС:** Windows, Linux, macOS
- **Python:** 3.8 или выше
- **RAM:** минимум 2 ГБ (рекомендуется 4+ ГБ)
- **Диск:** ~500 МБ для зависимостей + 43 МБ для модели
- **GPU:** не требуется (работает на CPU)

## 🎯 Готово!

После выполнения этих шагов приложение будет полностью работоспособно на новом ПК.

